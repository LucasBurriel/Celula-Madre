def unified_diff(old_text, new_text):
    """
    Compute unified diff between old and new text.
    Returns list of strings: ' line' (unchanged), '-line' (removed), '+line' (added).
    """
    pass
